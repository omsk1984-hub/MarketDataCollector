# Анализ метрик — прогон 2026-09-13 12:07 (после правок LOH / выброса батча)

**Источник данных:** `traces/counters_20260913_120717.csv` (24,931 строк, 22 сэмпла, интервал ~5с)
**Временной диапазон сэмплов:** `09:07:19` → `09:09:04` UTC (≈105 сек активного сбора)
**Метод сбора:** run_all_metrics.ps1 / collect-all (counters 5s + trace 90s + gcdump peak 50s + gcdump drained + drain 30s)
**Конфигурация:** Mode=all, TraceDuration=90s, GcDumpAtPeak=50s, DrainWait=30s, Trace Profile=gc-verbose

> Нагрузка та же, что в baseline 045558, 104057 и 112621 — **1,700,000 тиков**. Прямое пятистороннее сравнение.

---

## 0. Состав файлов прогона

| Файл | Размер |
|------|-------:|
| `counters_20260913_120717.csv` | 5.58 MB |
| `allocation_trace_20260913_120717.nettrace` | 32.15 MB |
| `allocation_trace_20260913_120717.nettrace.etlx` | 96.5 MB |
| `allocation_trace_20260913_120717.speedscope.json` | 2.95 MB |
| `allocation_trace_20260913_120717_topn.md` | 2.52 KB |
| `snapshot_peak_20260913_120717.gcdump` | 2.94 MB |
| `snapshot_drained_20260913_120717.gcdump` | 2.94 MB |
| `profiling_report_20260913_120717.md` | 1.49 KB |

Ошибки сбора: `*_err.log` = 0 байт, `profiling_report` = «Предупреждения сбора: нет» — сбор завершился штатно, 22 сэмпла.

---

## 1. Динамика нагрузки и дропы

| Метрика | Старт | Финал |
|---|---|:---:|
| `ticks_incoming_count_total` | 170,289 | **1,700,000** |
| `ticks_received_count_total` | 170,000 | **1,700,000** |
| `ticks_processed_count_total` | 165,010 | **1,649,032** |
| `ticks_dropped_count` (DropOldest) | 0 | **0** |
| `ticks_dropped_silently_count` | 0 | **0** |
| `processor_channel_backlog_count` (incoming−received) | 54 | **0** |

### Ключевые показатели
- **Вход в батч:** `received / incoming = 1,700,000 / 1,700,000 = 100%` ✅
- **Запись в БД:** `processed / incoming = 1,649,032 / 1,700,000 = 97.00%` ✅ (идентично 112621/104057)
- **Дропы:** 0 (как visible `DropOldest`, так и silent) ✅
- **Backlog канала:** 54 на старте → **0** на финале ✅ — начальное скопление рассосалось, канал читается полностью
- Пропускная способность: вход ≈ `1,700,000/105с ≈ 16,190 тик/с`; запись в БД ≈ `1,649,032/105с ≈ 15,705 тик/с`.

> ✅ **Стабильное здоровое состояние** — все 1.7M тиков вошли в канал, записано 97.00%, ноль потерь. Начальный backlog 54 тика незначителен и ушёл в 0 к финалу.

---

## 2. Дедупликация и инвариант баланса

| Канал | `deduplicated_cache` | `deduplicated_db` | Σ |
|:------|:---:|:---:|:---:|
| channel 0 | **50,966** | **2** | 50,968 |

**Проверка инварианта баланса тиков (по факту прошедшим канал данным):**
```
received − processed = 1,700,000 − 1,649,032 = 50,968
50,966 (cache) + 2 (db) = 50,968  ✅ удовлетворено ровно
```

- Инвариант `received − processed == deduplicated.cache + deduplicated.db` **выполнен идеально** (50,968 = 50,968), без расхождений.
- Отсев почти целиком выполняет in-process `DeduplicationCache` (50,966 из 50,968); `ON CONFLICT` в БД фильтрует лишь 2 тика (в 112621 — 8). Доля дедупликации стабильна (~3% от входа).
- `incoming − received = 0` — потерь вне дедупликации нет.

---

## 3. Анализ канала (backlog)

| Метрика | Старт | Макс | Финал |
|---|:---:|:---:|:---:|
| `processor_channel_backlog_count` (incoming−received) | 54 | ~54 | **0** |
| `processor_channel_fill_count_sum` (ch0) | 0 | 3,107 | 3,107 |
| `processor_channel_fill_count_count` (ch0) | 1 | 8 | 8 |
| `processor_channel_fill_level_count` (ch0) | 29 | 29 | 0 |
| `processor_batch_channel_fill_count_sum` | 0 | **1** | 1 |
| `processor_batch_channel_fill_count_count` | 1 | 7 | 7 |

- Backlog канала **54 на старте**, быстро сходится к **0** — консьюмер успевает читать все тики.
- Очередь батчей (`processor_batch_channel_fill`) за прогон суммарно лишь ~1 батч при 7 вставках → **avg ≈ 0** (в 112621 avg ≈ 9, в 094702 регресс ≈ 28). ⚠️→✅ **Средняя наполненность очереди батчей упала практически до нуля** по сравнению с прошлым прогоном — медленные вставки (см. §5) больше не накапливаются в очереди.
- **Вывод:** канал и очередь батчей не накапливаются; выбросы записи перестали генерировать бэклог.

---

## 4. WebSocket-подключения

| Символ | Сообщений (финал) | Доля |
|:---|:---:|:---:|
| `binance/btcusdt` | 588,380 | 34.6% |
| `binance/ethusdt` | 569,633 | 33.5% |
| `binance/solusdt` | 541,987 | 31.9% |

- Активных соединений: **3** (binance), распределение ровное (32–35%). Σ `ws_messages_received` ≈ 1.70M согласуется со входом тиков. Продюсер стабилен, не является ограничением.

---

## 5. Длительность записи батчей в БД

Параметры `ticks_batch_write_duration_milliseconds` (1,247 батчей):

| Показатель | min | p50-подобный | avg | max |
|---|:---:|:---:|:---:|:---:|
| duration, мс | 76.5 | **390.9** | **674.1** | **2,543.0** ✅ |
| inserted_count | 4,810 | 4,852 | 4,851 | 4,891 |

- **Средняя длительность батча ≈ 674 мс** (112621: 842 мс; 104057: 746 мс) — **улучшение**, лучший уровень среди текущих прогонов.
- **Максимум ≈ 2.54 с** (112621: 7.36 с; 104057: 2.54 с) — **крупный выброс прошлого прогона ушёл обратно к норме 104057**. Медиана 391 мс — в норме, выбросы редкие и локальные.
- Размер батча ~5,000 (адаптивный), вставлено в среднем 4,851 (~3% дубликатов — согласуется с dedup cache).
- **Эффективность вставки:** `inserted/batch ≈ 4,851/5,000 = 97%`.

> В `worker_out.log` видны `BulkCopy total` 150–445 мс с предупреждениями «>200 мс» на 12:07:42 (всплеск до 444 мс). Это соответствует max 2.54 с на уровне гистограммы (мс-разрешение), но без накопления очереди батчей (см. §3).

---

## 6. .NET Runtime метрики

| Метрика | Старт | Финал | Интерпретация |
|---|---|---|:---:|---|
| GC Allocations total | 84.7 MB | **544.8 MB** | прирост 460 MB за 1.7M тиков ≈ **271 Б/тик** ✅ |
| GC Collections gen0/gen1/gen2 | — | **1/1/4** | 4 Gen2 за ~105с — низко ✅ |
| GC Heap gen0/gen1/gen2 | — | 4.36 / 0.08 / 8.2 MB | Gen2 стабильна |
| GC Heap LOH | — | **18.9 MB** ✅ | **LOH упал ≈ в 2.1× vs 112621 (40.5 MB)** |
| GC Heap fragmentation LOH | — | **1.84 MB** ✅ | ≈10% LOH, ниже vs 112621 (5.24 MB) |
| GC Committed Memory | 207.3 MB | **221.5 MB** | Ниже и 112621 (237.4) и регресса (263.1) |
| GC Objects size (используется) | 84.9 MB | **25.7 MB** | Дренаж к финалу |
| GC duration total | 0 | **90.1 ms** | Паузы GC малые |
| Lock contention | 7 | **24** | Очень низкий ✅ (112621: 27; 104057: 2,480) |
| ThreadPool completed items | 77,902 | **1,127,238** | ~1.13M задач (112621: 0.88M) |
| ThreadPool queue length | 0 | 0 | Очередь пустая |
| ThreadPool threads | 7 | 3 | Дренаж |
| Exceptions | 32 | 82 | Низкий уровень |
| Timer count | 18 | 17 | Стабильно |

### Наблюдения
- ✅ **Аллокации на тик ≈ 271 Б** (112621: 277 Б) — стабильно низкие, улучшение от правок hot-path **сохранено** (baseline ~619 Б, ×2.3).
- ✅ **LOH упал до 18.9 MB** (112621: 40.5 MB) с фрагментацией 1.84 MB (~10%): рекомендация прошлого прогона отработана — источник крупных объектов/мусора устранён. Уровень LOH вернулся к 104057 (18.4 MB).
- ✅ **Lock contention 24** — порядки ниже 104057 (2,480), сохранён достигнутый уровень.
- ThreadPool completed items вырос (1.13M vs 0.88M) при тех же аллокациях — незначительное изменение в распределении работы на тик, не аномалия.

---

## 7. Бинарные файлы (topN из `allocation_trace_20260913_120717_topn.md`)

| # | Функция | Excl. % | Категория |
|:---:|---|:---:|---|
| 1 | `ManagedWebSocket.GetReceiveResult` | 33.3% | WS приём |
| 2 | `ManagedWebSocket.ReceiveAsync` | 21.62% (54.5% incl) | WS приём |
| 3 | `GetTaskForValueTaskSource` | 10.22% | async инфраструктура |
| 4 | `ArrayPool<byte>.Rent` | 9.66% | Аренда буферов |
| 5 | `String.Concat` | 8.65% | Строковые операции |
| 6 | `Decimal` ctor | 6.71% | Парсинг цен |
| 7 | `Encoding.GetBytes` | 1.24% | Кодировка |
| 8 | `RuntimeMetrics` ctor | 0.49% | Метрики |
| 9 | `TickData[].Rent` | 0.16% | Арена тиков |

> ✅ **Профиль стабилен** и совпадает с 112621: доминирует честный WS-приём (`GetReceiveResult` 33.3% + `ReceiveAsync` 21.6%) и парсинг (`Decimal` ctor 6.7%). `CancellationTokenSource.*` отсутствует в топ-N — правка CTS-горячего пути продолжает работать. Доля `Decimal` ctor снизилась (6.7% vs 14.4% в 112621) — позитивный сдвиг от парсинга к WS-инфраструктуре.

---

## 8. Технические аномалии сбора

1. Ошибки сбора отсутствуют (`*_err.log` = 0 байт), 22 сэмпла, профилирование без предупреждений.
2. `*.nettrace.etlx` 96.5 MB сконвертирован в speedscope 2.95 MB без сбоев.
3. Прогон завершился штатно, active connections = 3 на всём протяжении.

---

## 9. Сравнение с прошлым прогоном (112621) и baseline 045558

| Показатель | Baseline 045558 | 104057 | 112621 | **120717** | Оценка |
|---|:---:|:---:|:---:|:---:|:---:|
| Вход в канал | 100% | 100% | 100% | **100%** | ✅ стабильно |
| Записано в БД | 97.01% | 97.01% | 97.01% | **97.00%** | ✅ стабильно |
| Дропы (вкл. silently) | 0 | 0 | 0 | **0** | ✅ стабильно |
| Max backlog канала | 1 | 0 | 103→0 | **54→0** | ✅ микро-бэклог |
| Очередь батчей (avg) | ~9 | ~0.7 | ~9 | **~0** | ✅ **улучшение** |
| Max batch duration | 3.0s | 2.54s | 7.36s | **2.54s** | ✅ регресс убран |
| Avg batch duration | 820ms | 746ms | 842ms | **674ms** | ✅ **лучший** |
| Аллокации на тик | ~619 Б | ~598 Б | ~277 Б | **~271 Б** | ✅ сохранено ×2 |
| Lock contention | 2,392 | 2,480 | 27 | **24** | ✅ сохранено |
| LOH heap | 36.7 MB | 18.4 MB | 40.5 MB | **18.9 MB** | ✅ **улучшение** |
| Batch-гистограмма | 1092 | 1205 | 1164 | **1247** | — |

**Вывод сравнения:** прогон 120717 — **самый сбалансированный**. Подтверждены все улучшения 112621 (аллокации ~271 Б/тик, contention 24, отсутствие CTS в topN) и **устранены обе найденные в 112621 проблемы:**
- ✅ **LOH снижен 40.5 → 18.9 MB** (вернулся к уровню 104057);
- ✅ **max batch duration снижен 7.36 → 2.54 с**, avg batch 842 → 674 мс (лучший уровень), очередь батчей ~0;
- ✅ ноль потерь, стабильные 97% записи.

---

## 10. Ключевые проблемы и рекомендации

1. **⚠️ Остаточный выброс max batch duration 2.54 с.** Средняя 674 мс и медиана 391 мс в норме, очередь батчей ~0, риск дропов минимален. Рекомендуется продолжить наблюдение: если выбросы станут чаще — аудит причины медленной вставки (блокировка СУБД/checkpoint) через PerfView на момент выброса.
2. **✅ Зафиксировать стабильные улучшения:** аллокации ~271 Б/тик (×2 vs baseline), contention 24, LOH 18.9 MB, отсутствие CTS в topN. Не регрессировать.
3. **Наблюдать эффективность вставки 97%** (4,851/5,000) и стабильность дедупликации (~3% от входа, 50,966 кэш + 2 СУБД) — инвариант выполняется ровно.
4. **Optional:** при желании ещё снизить avg batch duration — смотреть гистограмму длительностей в `ticks_batch_write_duration_milliseconds` (по `channel_index/batch_size/inserted_count`) и `BulkCopy phase-timing` в `worker_out.log`.