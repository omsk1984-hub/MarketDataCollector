# Анализ метрик — прогон 2026-08-01 14:29:21 — 14:31:32

**Источник данных:** `traces/counters_20260801_142921.csv` (14,765 строк, ~16 сэмплов по ~7–8 сек, общая длительность ~131 сек)
**Дополнительно собрано:** `allocation_trace_20260801_142921.nettrace` (30.73 MB), `allocation_trace_20260801_142921.speedscope.json` (17.37 MB), `snapshot_peak_20260801_142921.gcdump` (2.61 MB), `snapshot_drained_20260801_142921.gcdump` (2.44 MB)
**Метод сбора:** `run_all_metrics.ps1` → `scripts/collect-all.ps1` (mode=all, trace 90s, gcdump peak 50s, drain 30s)
**Нагрузка:** **1,700,000 тиков** (3 символа binance: btcusdt/ethusdt/solusdt)
**Состав `traces/` подтверждён через `list_files`** — 6 файлов прогона 142921 присутствуют.

> **Ключевой результат прогона:** четвёртый прогон подряд с **нулевыми потерями**. `received == incoming == 1.7M`, канал дренируется до 0. Инвариант баланса тиков выполняется точно (51,004 == 51,004). ⚠️ **Главное — резкое снижение пика батч-очереди до ~5 батчей** (против ~40 в прогоне 141644): узкое место записи в БД на старте сглажено. Длительность записи батча стабильна (~80–105 мс), адаптивный размер батча не деградировал (держался 2500 весь прогон). Дедупликация практически целиком на in-process кэше.

---

## 1. Динамика нагрузки и дропы тиков

| Сэмпл | `ticks_incoming` | `ticks_received` | `ticks_processed` | `dedup_cache` | `dedup_db` | Backlog `incoming-received` |
|---|---|---|---|---|---|---|
| 14:29:39 | 222,723 | 210,000 | 203,675 | 6,324 | 1 | 12,723 |
| 14:30:15 (пик) | ~1.3M | ~1.3M | ~1.27M | ~37K | 5 | ~6.4K |
| 14:30:23 | 1,323,907 | 1,317,500 | 1,277,929 | 39,566 | 5 | 6,407 |
| 14:30:30 | 1,507,502 | 1,505,000 | 1,459,838 | 45,157 | 5 | 2,502 |
| 14:31:21 | **1,700,000** | **1,700,000** | **1,648,996** | **50,999** | **5** | **0** |
| 14:31:28 (финал) | 1,700,000 | 1,700,000 | 1,648,996 | 50,999 | 5 | 0 |

### Ключевые выводы
- ✅ **Дропов каналом нет.** Метрики `ticks_dropped_*` в CSV отсутствуют; `received == incoming == 1.7M`, финальный backlog = 0 (DropOldest не сработал). Четвёртый прогон подряд с нулевыми потерями.
- ✅ **Записано в БД: 1,648,996 / 1.7M = 97.00%** — стабильно на уровне прошлых прогонов.
- **Инвариант баланса выполняется точно:** `received − processed = 1,700,000 − 1,648,996 = 51,004` и `dedup_cache (50,999) + dedup_db (5) = 51,004` ✅ — без сбоев записи.
- **Пропускная способность:** вход ~13.6K тик/сек (1.7M/125с активной фазы), запись ~13.2K тик/сек.
- **Стартовый backlog входа** заметно ниже, чем в прошлом прогоне: пик бэклога ~6.4K тиков (14:30:23) против ~98.7K в прогоне 141644 — продюсер/консьюмер сбалансированы с самого начала. К 14:31:21 канал полностью дренирован.

---

## 2. Дедупликация (incoming − processed)

Разбивка расхождения `incoming − processed = 51,004` на два уровня:

| Уровень | Метрика | Значение (финал) | Доля от incoming | Доля от расхождения |
|---|---|---|---|---|
| **In-process** | `ticks_deduplicated_cache_count_total` | **50,999** | 3.000% | **99.99%** |
| **БД (ON CONFLICT)** | `ticks_deduplicated_db_count_total` | **5** | 0.0003% | 0.01% |
| **Итого** | | **51,004** | 3.00% | 100% |

### Выводы
- ✅ **Инвариант `received − processed == cache + db` выполняется точно** (51,004 == 51,004) — сбоев записи нет.
- ✅ **99.99% дубликатов отсеивает in-process `DeduplicationCache`** до записи в БД (50,999 из 51,004). Уровень `ON CONFLICT` почти не задействован (5 тиков) — кэш работает эффективно, не нагружая БД.
- Дедупликация полностью на единственном канале `channel_index=0`; дисбаланса по каналам нет.

---

## 3. Анализ канала (backlog) и батч-канала

`processor_channel_fill_count` (histogram, глубина входного канала в тиках):
- 14:29:39: `sum=145, count=2` → средняя глубина **~72** тика (стартовый бэклог умеренный).
- 14:30:30: `sum=156, count=7` → средняя **~22** тика — канал постепенно дренируется.
- Финал: глубина ~0 (канал пуст).

✅ **`processor_batch_channel_fill_count` (очередь батчей, ожидающих записи в БД) — ключевое улучшение прогона:**

| Сэмпл | sum / count | Средняя очередь | Пик (buckets) |
|---|---|---|---|
| 14:29:46 | 3 / 2 | ~1.5 | ≤5 батчей |
| 14:30:30 | 11 / 6 | ~1.8 | ≤5 батчей |
| 14:31:28 | ≤6 | ~1–2 | ≤5 батчей |

- ⚠️ В прогоне 141644 пик батч-очереди составлял **~40 батчей** (узкое место записи на старте). В текущем прогоне очередь **не превышала ~5 батчей** на всём протяжении — это **существенная регрессия к лучшему**: writer стабильно поспевал за продюсером.
- **Вывод о дисбалансе продюсер/консьюмер:** пикового дисбаланса на старте не было; конвейер сбалансирован с начала до конца. Причина стабильности — равномерная длительность записи батчей (~80–105 мс, без выбросов), см. раздел 5.

---

## 4. WebSocket-подключения

`ws_active_connections_count` = **3** (binance) весь прогон — без обрывов.

Распределение `ws_messages_received_count_total` по символам (финал):

| Символ | Сообщений | Доля |
|---|---|---|
| btcusdt | 590,371 | 34.7% |
| solusdt | 557,437 | 32.8% |
| ethusdt | 552,192 | 32.5% |
| **Итого** | **1,700,000** | 100% |

Распределение равномерное (±2 п.п.), дисбаланса WS-потоков нет.

---

## 5. Длительность записи батчей в БД

`ticks_batch_write_duration_milliseconds` (гистограмма по `batch_size=2500`, `inserted_count` 2402–2449). Сводка по фазам прогона:

| Фаза | Пример avg (count) | Эффективность inserted/batch |
|---|---|---|
| Начало (14:29:39) | ~78–96 мс (1–6) | 96.1–98.0% |
| Пик (14:30:15–23) | ~80–105 мс (6–24) | 96.3–97.6% |
| Финал (14:31:21–28) | ~80–105 мс (23–33) | 96.4–97.8% |

### Выводы
- **Средняя длительность батча ~80–105 мс при batch_size=2500** — стабильна на всём протяжении прогона, без медленных выбросов. Это контраст с прошлым прогоном, где на старте были скачки очереди из-за медленных батчей.
- **Эффективность вставки ~96–98%** (ON CONFLICT отсеивает ~2–4% в рамках батча) — на уровне прошлых прогонов.
- **Адаптивный размер батча:** `ticks_batch_adaptive_size` держался **2500 весь прогон** (в прошлом прогоне деградировал до 1425). Поскольку очередь батчей не накапливалась, механизм адаптивного снижения не потребовался — это признак здоровой конвейерной нагрузки.

---

## 6. .NET runtime метрики (первый vs финальный сэмпл)

| Метрика | 14:29:24 (старт) | 14:31:28 (финал) | Δ |
|---|---|---|---|
| `gc_allocations_size_bytes_total` | 20,140,216 | 1,422,047,024 | **+1.40 GB** (~11.3 MB/с) |
| `gc_collections` gen0 | 0 | 4 | +4 |
| `gc_collections` gen1 | 0 | 3 | +3 |
| `gc_collections` gen2 | 0 | **5** | +5 |
| `gc_duration_nanoseconds_total` | 0 | 90,674,000 | ~90.7 мс |
| `gc_heap_size` gen2 | — | 5,034,392 | ~4.8 MB |
| `gc_heap_size` loh | — | 21,594,256 | ~20.6 MB |
| `gc_heap_fragmentation` loh | — | 2,682,856 | ~2.6 MB |
| `gc_objects_size_bytes` | 20,161,456 | 24,318,152 | ↑ (после GC) |
| `gc_committed_memory_size_bytes` | — | 237,391,872 | ~226 MB |
| `exceptions_count_total` | 34 | 53 | **+19** |
| `monitor_lock_contention_count_total` | 2 | 1,314 | **+1,312** |
| `thread_pool_completed_items_count_total` | 126 | 2,258,375 | +2,258,249 |
| `thread_pool_queue_length` | 0 | 0 | 0 |
| `thread_pool_threads_count` | 6 | 5 | ↓ (пик 12 в 14:30:30) |
| `timer_count` | 9 | 16 | +7 |

### Выводы
- **Аллокации ~1.40 GB за прогон (~11.3 MB/с)** — высокая аллокационная нагрузка, но **ниже прошлого прогона (~14 MB/с)**. Снижение, вероятно, связано с отсутствием стартового дисбаланса и меньшим числом операций повторной записи.
- ⚠️ **Gen2-сборки (5) при gen1 всего 3** — необычное соотношение (как и в прошлом прогоне, 5 Gen2). Суммарные паузы GC ~90.7 мс. LOH ~20.6 MB с фрагментацией ~2.6 MB — умеренное GC-давление, без переполнения.
- ⚠️ **Lock contention +1,312 за прогон** — конкуренция за монитор сравнима с прошлым прогоном (+1,347). Не критично, но сохраняется как точка внимания (DeduplicationCache/батч-канал).
- **Исключения +19 за прогон** — заметно выше прошлого прогона (2). Требует уточнения, но не нарушает инвариант баланса (нет потерь при записи). Могут быть фоновые (парсинг, connection log).
- Thread pool: 2.26M завершённых задач при пике 12 потоков, очередь пула = 0 — пул не перегружен, спайков нет.

---

## 7. Технические аномалии сбора

- Сбор прошёл без обрывов: CSV полный (14,765 строк, финальный сэмпл 14:31:28), все бинарные файлы собраны (nettrace 30.73 MB, gcdump peak 2.61 MB / drained 2.44 MB).
- Метрики `ticks_dropped_*` в CSV отсутствуют — согласуется с нулевыми потерями (не признак обрыва).
- ⚠️ **Рост исключений (+19)** — потенциальная аномалия для уточнения (нет потерь, но стоит проверить логи на тип исключений).
- Бинарные `nettrace`/`gcdump` **не разбирались** — counters-анализ дал исчерпывающий ответ по потерям/балансу; для точечного разбора GC-давления доступен `allocation_trace_20260801_142921.nettrace` и снапшоты.

---

## 8. Сравнение с прошлым прогоном (20260801_141644)

| Показатель | 141644 | **142921 (текущий)** | Изменение |
|---|---|---|---|
| Нагрузка | 1.7M | 1.7M | = |
| Вход в канал | 100% (1.7M) | 100% (1.7M) | = |
| Записано в БД | 97.00% | **97.00%** | = |
| Дропы | 0 | **0** | = (✅ стабильно 4-й прогон) |
| Инвариант баланса | 51,115 == 51,115 | **51,004 == 51,004** | ✅ |
| dedup cache / db | 51,109 / 6 | 50,999 / 5 | cache доминирует |
| Стартовый backlog входа | ⚠️ до ~98.7K | **~6.4K** | ✅ улучшение |
| Пик батч-очереди | ⚠️ ~40 батчей | ✅ **~5 батчей** | **существенное улучшение** |
| Длительность батча | ~78–105 мс | ~80–105 мс | = (без выбросов) |
| Адаптивный размер батча | снижался до 1425 | 2500 весь прогон | ✅ улучшение |
| Аллокации | ~14 MB/с | ~11.3 MB/с | ✅ улучшение |
| Gen2-сборки | 5 | 5 | = |
| Исключения | +2 | ⚠️ +19 | **регрессия** |

### Выводы
- ✅ **Потери по-прежнему нулевые** — главный KPI стабилен четвёртый прогон подряд.
- ✅ **Ключевое улучшение:** пик батч-очереди упал с ~40 до ~5 батчей, стартовый backlog входа — с ~98.7K до ~6.4K. Узкое место записи в БД на старте сглажено: writer стабильно поспевал за продюсером, адаптивный размер батча не деградировал.
- ⚠️ **Единственная регрессия — рост исключений (+19 против +2).** Не влияет на потери, но требует проверки источника.

---

## 9. Ключевые проблемы и рекомендации

1. ⚠️ **Рост исключений (+19 за прогон)** — единственный новый сигнал. Проверить логи воркера/otel на тип и источник исключений (парсинг WS-сообщений, connection log, фоновые таймеры). Потерь нет, но на росте нагрузки это может стать проблемой.
2. **Gen2-сборки (5) при LOH ~20.6 MB / фрагментация LOH ~2.6 MB** — умеренно, но при росте нагрузки рискованно. Мониторить LOH/Gen2-фрагментацию; при необходимости — компактинг/пулы больших буферов. Доступен `nettrace` для точечного поиска топ-аллокаций.
3. **Lock contention +1,312 за прогон** — сохраняется (DeduplicationCache, батч-канал). Абсолютное значение некритично, но следить при масштабировании каналов.
4. **Позитив для закрепления:** нулевые дропы и точный инвариант баланса 4-й прогон подряд; пик батч-очереди снижен до ~5 (узкое место записи сглажено); аллокации снизились до ~11.3 MB/с; дедупликация почти целиком на кэше (99.99%). Текущую схему каналов/дедупликации менять не следует.
