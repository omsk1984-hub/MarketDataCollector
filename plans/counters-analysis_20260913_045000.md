# Анализ метрик — контрольный прогон 2026-09-13 04:24–04:25 (после C1/C2)

**Источник данных:** `traces/counters_20260913_042400.csv` (5.29 MB, 22 531 строка, 22 сэмпла, первый `01:24:03Z`, последний `01:25:48Z`)
**Дополнительно:** `profiling_report_20260913_042400.md`, `worker_out.log`, `allocation_trace_20260913_042400_topn.md`, `allocation_trace_20260913_042400.nettrace`, gcdump (peak/drained)
**Сравнение:** [список файлов traces/](traces) — актуальный прогон `042400`, предыдущий `035508`
**Проверяемые изменения:** C1 (переиспользование scope/repository на уровне Writer-цикла), C2 (trace sampling 100)

**Метод сбора:** Profiler (mode=all, trace 90s gc-verbose, gcdump 50/30), сбор без предупреждений (err-логи 0, `profiler_err.log`=0 байт)
**Нагрузка:** 1 700 000 тиков, 3 символа binance, `UseSingleConsumer`, `batchSize=5000`, `ProcessBatchTraceSampling=100`
**Активная фаза:** 04:23:50–04:25:17 (~87 с), далее дренаж (RPS=0), финал корректно (processed=1 648 783, backlog=0, dropped=0)

> **Ключевой результат:** C1 дал **лёгкое** улучшение batch duration (avg 739.8 мс), но **не устранил** накладную вокруг insert (чистый INSERT ≤298.7 мс). Заметно улучшились GC/LOH: GC-duration 72.5 ms, LOH-куча 22.0 MB, LOH-fragmentation 3.74 MB. Дропов по-прежнему **0** (7-й прогон подряд). ✅ В этом прогоне **инвариант дедупликации сходится точно** (Δ=0).

---

## 1. Динамика нагрузки и дропы

| Сэмпл (UTC) | `incoming` | `received` | `processed` | `dedup_cache` | `dedup_db` | Backlog | Дропы (bin/kraken/unknown) |
|---|---|---|---|---|---|---|---|
| 01:24:03 (старт) | 175 001 | 170 000 | 164 827 | 5 173 | 0 | 5 001 | 0/0/0 |
| 01:25:04 (конец активной) | 1 700 000 | 1 700 000 | 1 643 955 | — | — | 0 | 0 |
| 01:25:48 (финал) | **1 700 000** | **1 700 000** | **1 648 783** | **51 212** | **5** | **0** ✅ | **0/0/0** ✅ |

- **Funnel (финал):** вход в батч **100%** (received==incoming), записано в БД **97.00%**, дропы **0** (7-й прогон подряд).
- Пиковая пропускная: Incoming=24 503 msg/s, Processed=20 789 ticks/s (health 04:25:07).
- `worker_out.log` финал: `Incoming=1700000, received=1700000, processed=1648783, dropped=0, backlog=0, stop 9ms` — на уровне стопа всё сходится.

## 2. Дедупликация и инвариант

| Уровень отсева | Значение (финал) | Доля от `incoming − processed` |
|---|---|---|
| `dedup_cache` (in-process cache) | 51 212 | 99.99% |
| `dedup_db` (ON CONFLICT) | 5 | 0.01% |
| **Сумма** | **51 217** | — |

- Проверка инварианта: `received − processed = 1 700 000 − 1 648 783 = **51 217**`; `cache + db = 51 212 + 5 = **51 217**` → **равенство точное** ✅.
- Основной отсев дубликатов — in-process кэш (99.99%); `ON CONFLICT` = 5 — шум (0.01%).
- ✅ **Важная корректировка:** в файле [`plans/counters-analysis_20260913_042400.md`](counters-analysis_20260913_042400.md) инвариант был посчитан неверно (`52 117` → расхождение Δ=900). Фактический подсчёт по CSV даёт **точно 51 217** и **Δ=0**. Расхождения на уровне финального сэмпла **нет**.

## 3. Анализ канала / backlog

- `processor_channel_fill_count` sum = 0→12 (средняя **~1.4** — почти пусто), `processor_channel_fill_level_count` = 0→0 (уровень заполнения 0).
- `processor_batch_channel_fill_count` sum = 0→7 (средняя **~1.0 из 40**) — очередь батчей почти пуста, огромный запас. Коллектор фактически стримит батчи без накопления.
- Backlog (incoming−received) на старте 5 001, к финалу **0**. ✅ Продюсер успевает за консьюмером, узкого места на канале нет.

## 4. WebSocket-подключения (по символам)

| Символ | `ws_messages_received_count_total` | Доля |
|---|---|---|
| `btcusdt` | 582 538 | ~34% |
| `ethusdt` | 576 996 | ~34% |
| `solusdt` | 540 466 | ~32% |
| **Сумма** | **1 700 000** | 100% |

- Активных подключений: **3** (binance), отключений/реконнектов **0** (`3 connected, 0 disconnected`).
- Нагрузка распределена равномерно между символами.

## 5. Длительность записи батчей в БД

| Показатель | Значение |
|---|---|
| Всего батчей | 1 114 |
| `inserted` min/max/avg | 4 814 / 4 877 / **4 848** |
| `duration` min/max | 80.4 мс / **2 910.0 мс** |
| `duration` **avg** | **739.8 мс** |
| `duration` **p50** | **552.6 мс** |

- Эффективность вставки: `inserted/bs` = 4 848/5 000 = **~97%** (остальное — отсев кэшем/ON CONFLICT).
- ⚠️ **Max-хвост 2 910 мс** — выделяется на фоне p50 553 мс и avg 740 мс. Однако логи `BulkCopy phase-timing` показывают **чистый INSERT max = 298.7 мс** (04:24:47), все `tail=False` → **ни один чистый INSERT не превысил 1 с**. Хвосты 2.5–2.9 с создаются **накладной вокруг insert** (см. §7).

## 6. .NET runtime метрики (начало → конец)

| Метрика | Старт | Финал | Δ / интерпретация |
|---|---|---|---|
| `gc_allocations_size_bytes_total` | 144.7 MB | **1 088.1 MB** | +943 MB (~8.7 MB/с) — высокая аллокац. нагрузка, ≈ прошлого |
| `gc_collections` gen0/gen1/gen2 | — | **3/2/5** | 5 Gen2 (как обычно) |
| `gc_duration_nanoseconds_total` | 0 | **72.5 ms** | ✅ ниже прошлого (162.3 ms) — паузы GC минимальны |
| `gc_committed_memory_size_bytes` | 212.5 MB | 245.2 MB | +32.7 MB (прошлое +21.4) ⚠️ чуть выше |
| `gc_objects_size_bytes` | 145.0 MB | 26.8 MB | дренаж ~27 MB |
| `gc_heap_size` (gen0/gen1/gen2/**loh**/poh, финал) | — | 0.25/0.0007/**8.14/22.0/0.35 MB** | ✅ **LOH = 22.0 MB** (прошлое 24.2, база ~17) |
| `gc_heap_fragmentation` (loh, финал) | — | **3.74 MB** | ✅ ниже прошлого (4.53) |
| `gc_committed_memory` (финал) | — | **245.2 MB** | +32.7 MB ⚠️ чуть выше прошлых прогонов |
| `monitor_lock_contention_count_total` | 187 | **2 152** | +1 965 contention (~прошлого +2 091) |
| `thread_pool_completed_items_count_total` | 211 852 | 2 288 922 | +2 077 070 задач (~21.1К/с) — выше прошлого (~19.9К/с) |
| `exceptions_count_total` | 32 | 82 | +50 (низко) |
| `timer_count` | 18 | 17 | стабильно |

**Интерпретация:** ✅ GC-duration упала до 72.5 ms (против 162.3) — паузы GC не являются причиной медленной записи. ✅ LOH 22.0 MB / fragmentation 3.74 MB — держатся вблизи базы (17 MB), без аномалии. Contention ~стабильная (+1 965). Снижение GC-duration/LOH — вероятный вклад C1 (переиспользование scope убрало лишние аллокации DI-резолва). ⚠️ `committed_memory` выросла до 245 MB (+32.7) — следить, но остаётся далеко от аномальных значений.

## 7. Бинарные файлы (topN `gc-verbose`)

| # | Метод (exclusive) | Доля |
|---|---|---|
| 1 | `CancellationTokenSource.Register` | 18.08% |
| 2 | `CancellationTokenSource.CancelAfter` | 12.71% |
| 3 | `CancellationTokenSource.CreateLinkedTokenSource` | 10.99% |
| 4 | `TickData.WaitToReadAsync` | 10.83% |
| 5 | `ManagedWebSocket.ReceiveAsync` | 9.56% |
| 6 | `ManagedWebSocket.GetReceiveResult` | 6.48% |
| 7 | `__Canon.PopulateLiteralFields` | 6.26% |
| 9 | `.ctor(Decimal, Span<int16>)` | **4.08%** |

- ⚠️ **CTC (поз. 1,2,3,4) ≈ 52.6% exclusive — сохраняется 7 прогонов подряд.** Профиль `gc-verbose` по-прежнему ненадёжен для CTC: сам EventPipe генерирует CTC; `TickData.WaitToReadAsync` (flush-путь) в hot path не зовётся при `fills: 0%`. **Требуется `contention-cpu` для подтверждения** (артефакт инструментации vs real hot path). `Decimal` 4.08% — шум по сравнению с 2.2% прошлого (не регрессия, ранее был 7-13%).

## 8. Технические аномалии сбора

- Сбор штатный: `profiler_err.log`, `worker_err.log`, `fake_server_err.log` — все **0 байт**; `profiling_report` — «Нет предупреждений».
- `worker_out.log` финал корректен: `processed=1648783, dropped=0, backlog=0`, `StopProcessingAsync` 9 ms, корректный stop 3 WS-клиентов.
- ✅ **Инвариант в финальном сэмпле сходится точно (Δ=0)** — фактические данные из CSV (расхождение Δ=900, указанное в предыдущем отчёте `042400.md`, — ошибка вычисления, исправлена здесь).

## 9. Сравнение с прошлым прогоном (035508 → 042400)

| Показатель | 03:55 (A1) | **04:24 (C1/C2)** | Δ |
|---|---|---|---|
| Batch count | 1 103 | **1 114** | +11 |
| `inserted` avg | 4 850 | **4 848** | ≈ |
| duration **avg** | 764.2 мс | **739.8 мс** | **−3.3%** ✅ |
| duration **p50** | 579.9 мс | **552.6 мс** | **−4.7%** ✅ |
| duration **max** | 2 551 мс | **2 910 мс** | ⚠️ +14% |
| Пик write throughput | ~30.5К/с | **~25.2К/с** | ≈ (нагрузка ~25К) |
| processed % | 97.00 | **97.00** | ≈ |
| Дропы | 0 | **0** ✅ (7-й подряд) |
| LOH heap, MB | 24.2 | **22.0** | ✅ −9% |
| GC duration, ms | 162.3 | **72.5** | ✅ −55% |
| Contention Δ | +2 091 | **+1 965** | ✅ −6% |

**Итог:** C1 дал лёгкое улучшение batch duration (avg −3.3%, p50 −4.7%), заметное улучшение GC-duration (−55%) и LOH (−9%). Max вырос (+14%) — единичный хвост, чистый INSERT всё равно ≤300 мс. Улучшения по GC/LOH — вероятный вклад переиспользования scope (C1).

## 10. Ключевые проблемы и рекомендации (по приоритету)

1. ⚠️ **[P1 — улучшено, не устранено полностью]** Суммарно A1+C1+C2: batch avg **949→740 мс (−22%)**, write throughput **+37%**, GC-duration **162→72 ms**, LOH **вблизи базы (22 MB)**. Чистый INSERT быстр (≤300 мс). **Дальнейшая оптимизация** — не SQL, а **изоляция Npgsql-записи от общего пула** (выделенный thread / синхронное выполнение insert вне async-продолжений). Это отдельное перепроектирование (рискованно, требует отдельного плана и прогона).
2. ⚠️ **[P2]** CTC ≈53% в topN `gc-verbose` 7 прогонов подряд — **требуется прогон `contention-cpu`** для окончательного вердикта (артефакт vs real). Рекомендую `.\start_loadtest.ps1 -TraceProfile contention-cpu` (с согласия на нагрузку).
3. ✅ **[P1-плюс] LOH/GC в норме** — 22 MB / 72 ms / fragmentation 3.74 MB; контроль продолжать (лом вблизи базы).
4. ⚠️ **committed_memory выросла до 245 MB (+32.7 MB)** — следить в следующих прогонах; значение всё ещё умеренное, но тренд роста за 2 прогона стоит контролировать.
5. ✅ **Инвариант дедупликации сходится точно (Δ=0)** — документально исправить расхождение, указанное в [`042400.md`](counters-analysis_20260913_042400.md) (там ошибочно Δ=900).
6. **[P5]** Лог `tail` (A2) подтверждает: ни одного insert >1 с за весь прогон — хвосты 2.9 с это чисто клиентская накладная, не SQL.