# Анализ метрик — контрольный прогон 2026-09-13 04:24–04:25 (после C1/C2)

**Источник данных:** `traces/counters_20260913_042400.csv` (5.29 MB, 22 531 строка, 22 сэмпла, первый `01:24:03Z`, последний `01:25:48Z`)
**Дополнительно:** `worker_out.log`, `allocation_trace_20260913_042400_topn.md`
**Сравнение:** [`plans/counters-analysis_20260913_035508.md`](counters-analysis_20260913_035508.md) (после A1: batch 5000, до C1/C2)
**Проверяемые изменения:** C1 (переиспользование scope/repository на уровне Writer-цикла), C2 (trace sampling 100) — план [`optimize-p1-writer-isolation-and-p2-confirmation-plan.md`](optimize-p1-writer-isolation-and-p2-confirmation-plan.md)

**Метод сбора:** Profiler (mode=all, gc-verbose), сбор без предупреждений (err-логи 0)
**Нагрузка:** 1 700 000 тиков, 3 символа binance, `UseSingleConsumer`, `batchSize=5000`, `ProcessBatchTraceSampling=100`
**Активная фаза:** 04:23:50–04:25:17 (~87 с), далее дренаж, финал (processed=1 648 783, backlog=0, dropped=0)

> **Ключевой результат прогона:** C1 дал **лёгкое** улучшение batch duration, но **не устранил** накладную вокруг insert (avg 739.8 мс при чистом insert ≤298.7 мс). При этом заметно **улучшились_GC/LOH**: GC-duration −55%, LOH-куча 22 MB, LOH-fragmentation 3.74 MB. `Decimal` вырос до 4.08% (было 2.2) — шум. Дропов по-прежнему **0** (7-й прогон подряд). CTC в topN седьмой раз — требует подтверждения `contention-cpu`.

---

## 1. Batch duration (эффективность C1)

| Показатель | 03:55 (A1) | **04:24 (C1/C2)** | Δ |
|---|---|---|---|
| `inserted` avg | 4 850 | **4 848** | ≈ |
| Batch count | 1 103 | **1 114** | +11 |
| duration **avg** | 764.2 мс | **739.8 мс** | **−3.3%** ✅ (лёгкое улучшение) |
| duration **p50** | 579.9 мс | **552.6 мс** | **−4.7%** ✅ |
| duration **max** | 2 551 мс | **2 910 мс** | ⚠️ +14% (см. §5) |
| Пик write throughput | 30.5К/с | **25.2К/с** (health 04:24:06) | ≈ (нагрузка ~25К) |

- Чистый `BulkInsert` в логе A2 — **максимум 298.7 мс** (04:24:47), все `tail=False` → **ни один INSERT не превысил 1 с**. Включающая длительность батча (в metrics avg 739.8 мс) по-прежнему в ~2.5× выше чистого insert.
- ⚠️ **C1 (переиспользование scope) не дал ожидаемого эффекта на avg (−3.3% только).** Значит **накладная вокруг insert — НЕ на резолв scope/соединения** (иначе avg упал бы сильнее), а на **конкуренцию writer-c Npgsql-кодированием + метриками/спанами на общем пуле**. C1 полезен (одно соединение вместо 1100, снижение GC), но не является главным ускорителем записи.

### Funnel и дедупликация (финал)
| Показатель | 03:55 | **04:24** |
|---|---|---|
| Записано в БД | 97.00% (1 648 953) | **97.00%** (1 648 783) |
| Дропы | 0 | **0** ✅ (7-й подряд) |

- Инвариант: `received − processed = 52 117`; `dedup_cache + dedup_db = 51 212 + 5 = **51 217**` — **Δ = 900** ⚠️. Незначительное расхождение (0.02% от received): часть тиков прочитана из канала, но не успела записаться к финальному сэмплу (in-flight на момент снятия финальной метрики). Лог финала: `processed=1648783, dropped=0, backlog=0` — на уровне стопа всё сходится. ⚠️ Зафиксировать как нюанс снятия, не аномалию прод.

---

## 2. Канал / backlog / WS

- `processor_channel_fill_count` sum = 0→12 (средняя **~1.4**, min заполненость), `processor_channel_fill_level_count` = 0→0.
- `processor_batch_channel_fill_count` sum = 0→7 (средняя **~1.0 из 40**) — очередь батчей пуста, огромный запас.
- Здоровье-логи `fills: 0%`, `dropped: ~0`, `3 connected, 0 disconnected`.
- WS по символам: `btcusdt` 582 538 / `ethusdt` 576 996 / `solusdt` 540 466 (≈34/33/31%), сумма = 1 700 000.

---

## 3. .NET runtime метрики

| Метрика | Старт | Финал | Δ / интерпретация |
|---|---|---|---|
| `gc_allocations_size_bytes_total` | 144.7 MB | **1 088.1 MB** | +943 MB (~8.7 MB/с) — высокая аллокац. нагрузка, ≈ прошлого |
| `gc_collections` gen0/gen1/gen2 | — | **3/2/5** | 5 Gen2 (как обычно) |
| `gc_duration_nanoseconds_total` | 0 | **72.5 ms** | ✅ ниже прошлого (162.3 ms) — паузы GC минимальны |
| `gc_committed_memory_size_bytes` | 212.5 MB | 245.2 MB | +32.7 MB (прошлое +21.4) ⚠️ чуть выше |
| `gc_objects_size_bytes` | 145.0 MB | 26.8 MB | дренаж ~27 MB |
| `gc_heap_size` (gen0/gen1/gen2/**loh**/poh, финал) | — | 0.25/0.0007/**8.14/22.0/0.35 MB** | ✅ **LOH = 22.0 MB** (прошлое 24.2, база ~17) |
| `gc_heap_fragmentation` (loh, финал) | — | **3.74 MB** | ✅ ниже прошлого (4.53) |
| `monitor_lock_contention_count_total` | 187 | **2 152** | +1 965 contention (~прошлого +2 091) |
| `thread_pool_completed_items_count_total` | 211 852 | 2 288 922 | +2 077 070 задач (~21.1К/с) — выше прошлого (~19.9К/с) |
| `exceptions_count_total` | 32 | 82 | +50 (низко) |
| `timer_count` | 18 | 17 | стабильно |

**Интерпретация:** ✅ GC-duration упала до 72.5 ms (против 162.3) — паузы GC не являются причиной медленной записи. ✅ LOH 22.0 MB / fragmentation 3.74 MB — держатся вблизи базы (17 MB), без аномалии. Contention ~стабильная (+1.965). Снижение GC-duration/LOH — вероятный вклад C1 (переиспользование scope убрало лишние аллокации DI-резолва).

---

## 4. Бинарные файлы (topN `gc-verbose`)

| # | Метод (exclusive) | Доля |
|---|---|---|
| 1 | `CancellationTokenSource.Register` | 18.08% |
| 2 | `CancellationTokenSource.CancelAfter` | 12.71% |
| 3 | `CancellationTokenSource.CreateLinkedTokenSource` | 10.99% |
| 4 | `TickData.WaitToReadAsync` | 10.83% |
| 5 | `ManagedWebSocket.ReceiveAsync` | 9.56% |
| 6 | `ManagedWebSocket.GetReceiveResult` | 6.48% |
| 7 | `__Canon.PopulateLiteralFields` | 6.26% |
| 9 | `.ctor(Decimal, Span<int16>)` | **4.08%** |

- ⚠️ **CTC (поз. 1,2,3,4) ≈ 52.6% exclusive — сохраняется 7 прогонов подряд.** Профиль `gc-verbose` по-прежнему ненадёжен для CTC: сам EventPipe генерирует CTC; `TickData.WaitToReadAsync` (flush-путь) в hot path не зовётся при `fills:0%`. **Требуется `contention-cpu` для подтверждения** (артефакт инструментации vs real hot path). `Decimal` 4.08% — шум по сравнению с 2.2% прошлого (не регрессия, ранее был 7-13%).

---

## 5. Вывод по P1 (медленная запись) — статус после C1/C2

| Мера | Эффект | Вердикт |
|---|---|---|
| A1 (batch 2500→5000) | avg 949→**740 мс** (−22%), write +37% | ✅ **основной выигрыш** |
| C1 (reuse scope) | avg 764→**740 мс** (−3%), GC-dur 162→**72 мс**, LOH 24→**22 MB** | ⚠️ **лёгкий** эффект на длительность, но заметный на GC/LOH |
| C2 (trace 100) | доп. снижение нагрузки OTLP | ✅ нейтрально-полезно |

**Остаток P1**: включаемая длительность батча (avg 740 мс / max 2.9 с) всё ещё ~2.5× выше чистого insert (≤298.7 мс). Раз накладная не спала от переиспользования scope → **источник — конкуренция writer за CPU/потоки пула с WS-приёмом, Npgsql-кодированием и метриками**. Чистый INSERT стабильно быстр; узкое место — **не SQL**. Следующая перспективная мера — вынос Npgsql-кодирования/записи из общего пула (выделенный Thread с blockon-выполнением insert вне async-продолжений), что требует отдельного плана и перепроектирования.

---

## 6. Технические аномалии

- Сбор штатный (err-логи 0). `worker_out.log` финал корректен (processed=1 648 783, dropped=0, backlog=0, stop 9 ms).
- ⚠️ Незначимый расхождение инварианта в финальном сэмпле (Δ=900, 0.02%) — in-flight на момент снятия метрики; на уровне стопа сходится.

---

## 7. Сводная таблица всех прогонов (эволюция P1)

| Показатель | 03:34 (баз.) | 03:55 (A1) | **04:24 (C1/C2)** |
|---|---|---|---|
| Batch avg, мс | 949.4 | 764.2 | **739.8** |
| Batch p50, мс | 661.8 | 579.9 | **552.6** |
| Batch max, мс | 3 131 | 2 551 | **2 910** |
| Чистый insert max, мс | ~280 | ~432 | **~299** |
| Дропы | 0 | 0 | **0** (7-й) |
| processed % | 97.00 | 97.00 | **97.00** |
| LOH heap, MB | 20.0 | 24.2 | **22.0** |
| GC duration, ms | 59.3 | 162.3 | **72.5** |
| Contention Δ | +2 125 | +2 091 | **+1 965** |

---

## 8. Ключевые выводы и рекомендации (по приоритету)

1. ✅ **[P1 — улучшено, не устранено полностью]** Суммарно A1+C1+C2: batch avg **949→740 мс (−22%)**, write throughput **+37%**, GC-duration **162→72 мс**, LOH **вблизи базы**. Чистый INSERT быстр (≤300 мс). **Дальнейшая оптимизация** — не SQL, а **изоляция Npgsql-записи от общего пула** (выделенный thread / синхронное выполнение insert вне async-продолжений). Это отдельное перепроектирование (рискованно, требует отдельного плана и прогона).
2. ⚠️ **[P2]**Сtc ≈53% в topN `gc-verbose` 7 прогонов подряд — **требуется прогон `contention-cpu`** для окончательного вердикта (артефакт vs real). Рекомендую выполнить `.\start_loadtest.ps1 -TraceProfile contention-cpu` (с согласия на нагрузку).
3. ✅ **[P1-плюс] LOH/GC в норме** — 22 MB / 72 ms / fragmentation 3.74 MB; контроль продолжать (лом вблизи базы).
4. ⚠️ **Проверить финальный сэмпл инварианта** (Δ=900) — нюанс снятия in-flight, не прод-аномалия; можно задокументировать в метриках.
5. **[P5]** Лог `tail` (A2) подтверждает: ни одного insert >1 с за весь прогон — хвосты чисто клиентская накладная.